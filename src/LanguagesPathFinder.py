# Retrouver le chemin de traduction pour arriver a un autre langage

import HiboukStorePacketManager

def GetPath(StartID: int, EndID: int) -> list[int]:
    """
        Param:
            StartID : ID du langage de base
            EndID : ID du langage de base
        Return:
            Liste des langage intermediaires
    """
    #### A FAIRE (checker si le packet existe)

    return [int(StartID), int(EndID)]